
exports.receive = {
  'progress': 'progress'
};

exports.progress = function (msg, done) {
  console.log('listener progress %d', msg.data.value);

  if (msg.data.value >= 100) {
    console.log('all done');
    this.ctx.system.shutdown();
  }

  done();
};
